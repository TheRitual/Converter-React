import "./style.css";

const List = () => {   
        return (
            <section className="list">
                <h2 className="converter__header">Saved</h2>                
            </section>);
};

export default List;